'use strict';

// strict mode shows error in a understanding way

    var value = 10;
console.log(value + 1);


 const interface = 'Audio';// SyntaxError: Unexpected strict mode reserved word

// var if = 'a';
// Unexpected token 'if'